# 需求

设置阈值，峰值低于该阈值时，App不显示心率值。

## 需求分析

发送给App的ECG样板是down-sampling的，5点平均，所以在App一侧检查maximal会有较大误差，考虑在mcu一侧实现功能。

在mcu一侧，内存资源已经很匮乏，不适合做保存很多数据的sliding window；考虑一个简单的算法，定义为在最大所需的qrs peak间隔内，至少有一个数据点幅度超过预设的阈值，才向App发送智柔算法计算的心率值，否则只发送一个无效值，例如255。

最大所需的qrs peak间隔，考虑最低心率为30，则2秒钟一个peak，在250sps下这是500个sample。算法如下：

1. 预定义一个最小幅值，命名为min_amp，类型为int（或long long），初始化为-1；
2. 在

